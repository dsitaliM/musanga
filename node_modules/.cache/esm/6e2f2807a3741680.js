let express,ArtistModel;_a37‍.x([["default",()=>_a37‍.o]]);_a37‍.w("express",[["default",["express"],function(v){express=v}]]);_a37‍.w("../models/artist",[["default",["ArtistModel"],function(v){ArtistModel=v}]]);

const artistRouter = express.Router();

artistRouter
    .get('/artists', (req, res) => {
        ArtistModel.find({}, (err, artists) => {
            res.json(artists);
        });
    })
    .post((req, res) => {
        let artist = new ArtistModel(req.body);
        artist.save();
        res.status(201).send(artist);
    })
    .get('/artists/search/:artistId', (req, res) => {
        ArtistModel.findById(req.params.artistId, (err, artist) => {
            res.json(artist);
        });
    })
    .put((req, res) => {
        ArtistModel.findById(req.params.artistId, (err, artist) => {
            //TODO
            artist.save();
            res.json(artist);
        });
    })
    .delete((req, res) => {
        ArtistModel.findById(req.params.artistId, (err, artist) => {
            artist.remove(err => {
                if (err) {
                    res.status(500).send(err);
                } else {
                    res.status(204).send('removed');
                }
            });
        });
    });

_a37‍.d(artistRouter);
