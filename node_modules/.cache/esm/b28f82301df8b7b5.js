let express,AlbumModel;_1df‍.x([["default",()=>_1df‍.o]]);_1df‍.w("express",[["default",["express"],function(v){express=v}]]);_1df‍.w("../models/album",[["default",["AlbumModel"],function(v){AlbumModel=v}]]);

const albumRouter = express.Router();

albumRouter
    .get('/albums', (req, res) => {
        AlbumModel.find({}, (err, albums) => {
            res.json(albums);
        });
        //res.send("Yep it's working");
    })
    .post('/albums', (req, res) => {
        let album = new AlbumModel(req.body);
        album.save();
        res.status(201).send(album);
    })
    .get('/album/search/:albumId', (req, res) => {
        AlbumModel.findById(req.params.albumId, (err, album) => {
            res.json(album);
        });
    })
    .put((req, res) => {
        AlbumModel.findById(req.params.albumId, (err, album) => {
            //TODO
            album.save();
            res.json(album);
        });
    })
    .delete((req, res) => {
        AlbumModel.findById(req.params.albumId, (err, album) => {
            album.remove(err => {
                if (err) {
                    res.status(500).send(err);
                } else {
                    res.status(204).send('removed');
                }
            });
        });
    });

_1df‍.d(albumRouter);
