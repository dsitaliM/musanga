let artistsSchema,mongoose;_14e‍.x([["default",()=>_14e‍.o]]);_14e‍.w("./artists.model",[["default",["artistsSchema"],function(v){artistsSchema=v}]]);_14e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);


artistsSchema.statics = {
    create: (data, cb) => {
        let artist = new this(data);
        artist.save(cb);
    },

    get: function(query, cb) {
        this.find(query, cb);
    },

    getByName: (query, cb) => {
        this.find(query, cb);
    },

    update: (query, updateData, cb) => {
        this.findOneAndUpdate(query, { $set: updateData }, { new: true }, cb);
    },

    delete: (query, cb) => {
        this.findOneAndDelete(query, cb);
    }
};

const artistsModel = mongoose.model('Artists', artistsSchema);
_14e‍.d(artistsModel);
